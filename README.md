# Sudo with TouchID

This repo contains a shell script which enables authentication with TouchID on supported Macs when running `sudo`.

To activate this functionality, simply run

```bash
sudo ./sudo_touch_id.sh
```
